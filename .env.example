APP_ENV=local
LOG_LEVEL=INFO

# Optional — set all to enable automatic function calling via Azure OpenAI.
# Without these, the kernel + native plugin still run (mock mode).
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-10-21
